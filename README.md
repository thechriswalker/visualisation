# create visualisation video from audio file

Put an audio file in the `test/` dir.

```
go run *.go -audio test/audio.file
```

Ouput in `output/output.mkv`

You can control the output with `-video path/to/output` option
